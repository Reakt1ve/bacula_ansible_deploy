FileSet {
        Name = "Set_apk_dko_tematika"
        Include {
                Options {
                        signature = MD5
                        Compression = GZIP
                        recurse = yes
                        aclsupport = yes
                        xattrsupport = yes
                }
                File = /primetech
                File = /etc/init.d/update_server
                File = /etc/init.d/tematika
        }
}

